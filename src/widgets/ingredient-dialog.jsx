import { Modal } from "@mui/material";
import { useState } from "react";

export default function IngredientDialog () {
  const [open, setOpen] = useState(false)
  return <Modal open={open}>zzz</Modal>
}